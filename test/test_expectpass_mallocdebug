#!/bin/bash
cd $(dirname "$0")
cd ..
export LD_LIBRARY_PATH=.:~/.local/lib:/usr/local/lib
export ASCENDLIBRARY=models
export ASCENDSOLVERS=solvers/ipopt:solvers/qrslv:solvers/lrslv:solvers/dopri5:solvers/ida:solvers/radau5:solvers/ipslv:solvers/cmslv:solvers/conopt
test/test -r mallocdebug

# currently failing tests:
# solver_conopt solver_qrslv 
# compiler_blackbox system_link 
# integrator_ida

# tests that pass but with memory leaks:
# 
# solver_fprops
# linear_qrrank
#
# excluded due to known process-lifetime allocations in fundamental type
# descriptors during Asc_CompilerInit/DefineFundamentalTypes:
# compiler_instantiate_anontype

# suppressed for other reasons:
# compiler_qlfdid (didn't have PCRE enabled atm)
