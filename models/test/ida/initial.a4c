REQUIRE "ivpsystem.a4l";

MODEL ida_initial_decay;
	y IS_A solver_var;
	t IS_A solver_var;

	dyn: der(y) = -4 * y;
	INDEPENDENT t;
	INITIAL
		y0: y = 1;

METHODS
	METHOD on_load;
		FIX t;
		t := 0;
		FREE der(y);
	END on_load;
END ida_initial_decay;
