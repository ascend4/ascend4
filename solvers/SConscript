Import('env')
extenv = env.Copy()

subdirs = [
	'qrslv','conopt','cmslv','lrslv','lsode','ida','ipopt'
]
#'tron',

for d in subdirs:
	extenv.SConscript(d+'/SConscript',['env']);

# vim: set syntax=python:
