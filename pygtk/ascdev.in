#!/bin/sh

export ASC_START=$PWD
cd @SOURCE_ROOT@/pygtk
export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:@SOURCE_ROOT@"
export ASC_DEV=1

if [ "x$ASC_GDB" != "x" ]; then
	cmd="gdb --symbols=../@ASC_SHLIBPREFIX@ascend@ASC_SHLIBSUFFIX@ --args python ascend"
else
	cmd="./ascend"
fi

$cmd $@
